linters-settings:
linters:
  disable-all: true
  enable:
    - depguard
    - dogsled
    - dupl
    - errcheck
    - gochecknoinits
    - goconst
    - gocritic
    - gocyclo
    - gofmt
    - goimports
    - golint
    - goprintffuncname
    - gosec
    - gosimple
    - govet
    - ineffassign
    - interfacer
    - lll
    - misspell
    - nakedret
    - scopelint
    - staticcheck
    - structcheck
    - stylecheck
    - typecheck
    - unconvert
    - unparam
    - unused
    - varcheck
    - whitespace
    - gocognit
    - godox
    - maligned
    - prealloc
    
# disabled ones
    # - bodyclose
    # a lot of false positives
    # - deadcode
    # function is too long
    # - funlen
    # * is a global variable
    # - gochecknoglobals
    # expression shouldn't be cuddled
    # - wsl
    # magic numbers
    # - gomnd
    #
    # - rowserrcheck
